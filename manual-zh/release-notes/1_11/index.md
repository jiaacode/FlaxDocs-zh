# Flax 1.11 发布说明

## 亮点

### 自定义着色模型

![赛璐珞风格渲染](media/cel-shading.gif)

现在，Flax 支持添加**自定义着色模型**用于光照计算，**无需修改引擎源代码**。为风格化游戏制作自定义卡通着色或逼真的各向异性着色比以往任何时候都更容易。上面的视频展示了一个简单的赛璐珞风格渲染，带有对世界光照变化做出反应的轮廓，这是在材质图内实现的。要了解更多信息，请参阅专门的[教程](../../graphics/shaders/cel-shading.md)。

### 贴花层

![贴花层](media/decal-layers.png)

此版本中的另一个重要功能是通过模板缓冲区在着色器中进行逐像素对象遮罩。这使我们能够**根据下方的对象来遮罩贴花**。这样的功能允许仅在地面环境上绘制弹孔贴花，使用遮罩通过湿水坑贴花来丰富地形表面细节，而不会影响附近的其他对象。后期处理着色器也可以访问这些数据，以创建对象轮廓或高亮。

### 新的内存性能分析器

![编辑器中的新内存性能分析器](media/memory-profiler-editor.png)

Flax 现在有了新的**低级内存跟踪**工具，可以注册和跟踪引擎或游戏进行的任何内存分配。分配被分组到层级类别中，以提供更好的概览和对各种系统（如图形、物理或脚本）的洞察。这个强大的系统在将游戏移植到内存有限的平台（如移动设备或掌机）时非常有用。

![内存转储命令](media/memory-dump-command.png)

此功能附带了一套出色的工具，例如编辑器性能分析器集成，将类别分组到带有高亮和图表的表格中。新的 `ProfilerMemory.Dump` 命令将内存分配统计信息（按组）转储到日志中，以便快速调查。

### GPU 粒子性能改进

<center>
<video src="media/flax-game-particles.mp4" autoplay muted loop width="100%"></video>
<em>使用 Flax 制作的游戏在 Nintendo Switch 上运行。</em>
</center><br>


如果你的项目使用 GPU 粒子，那么在 1.11 版本上它应该运行得快得多！渲染和模拟都已重构，以更好地在不同硬件上扩展。GPU 粒子排序并行运行，并且对于小型发射器（少于 `2048` 个粒子）具有非常快的路径。最后，通过使用 **UAV 重叠和更优化的内存屏障**，GPU 执行得到了改进。

### 粒子发射器调试绘制

![粒子发射器调试绘制](media/particle-shapes.png)

粒子系统现在将为 `Set Position` 模块显示调试绘制形状。你可以通过取消选中粒子系统 Actor 上的 `Show Debug Draw` 复选框来隐藏它。

对于更高级的效果，粒子发射器编辑器中的新 Layout 选项卡显示粒子的布局和内存信息。

![粒子数据布局](media/particle-data-layout.png)

### Tracy 中的 GPU 性能分析器

![Tracy 中的 GPU 性能分析器](media/tracy-gpu.png)

在此版本中，我们添加了 GPU 性能分析与 [Tracy](../../editor/profiling/tracy.md) 性能分析器的集成。GPU 性能时间线中支持 D3D11、D3D12 和 Vulkan 图形 API。集成会自动插入计时器查询来计算各种事件的持续时间。此外，Tracy 还提供了显示与给定 GPU 工作事件相对应的 CPU 部分的功能。这可用于分析连接设备（包括移动设备和主机开发套件）上的渲染性能。

### Visject 改进

<center>
<video src="media/node-formatting-tools.mp4" autoplay muted loop height="400" />
<em>新的节点格式化选项</em>
</center><br>


Flax 有自己的图编辑工具，称为 Visject。它用于引擎的许多领域，从材质和粒子到动画。在 1.11 中，添加了许多新功能，并改进了一些现有功能，使得使用 Visject 的资源**编辑起来更加快速和容易**。

值得注意的更改：
* 加法和减法框选，
* 新的节点格式化和对齐选项，
* 节点连接可以用鼠标拖动移动，
* Time 节点现在包含缩放和未缩放的时间值，

### 视口图标缩放

![编辑器图标](media/editor-icons.gif)

视口图标现在根据它们到摄像机的距离改变大小。当它们远离摄像机时，这大大提高了它们的可见性。

### 新的键盘快捷键

使用鼠标完成所有操作可能会大大降低你的速度。这就是为什么我们**添加了一批新的键盘快捷键**。现在，你可以通过按 *Control + Number* 在编辑器中的窗口之间切换，或通过 *Control + Backquote*（`~` 键）快速访问调试控制台文本输入。调试视图模式和某些常用的视图标志也有了它们自己的快捷键（*Alt + Number* 用于视图模式，*Control + Shift + Number* 用于视图标志）。使用这些比通过视口中的 `View -> Debug View`/`View Flags` 选择视图模式要快得多。

当然，所有这些快捷键**都可以在编辑器设置中自定义**（说到这个：你现在可以通过按 *Control + Comma* 访问它们）。

### 异步场景加载

![异步场景加载](media/async-scenes-loading.png)

场景加载已重构，通过时间切片使用**时间预算（每帧）**。它是可配置的，并且**消除了打开地图或流式传输关卡时的卡顿**。这使加载大型关卡更加平滑，并且该代码的更多部分现在异步运行。默认情况下，它被配置为使用总帧时间的最大 30%（例如，在 60fps 下为 4.8ms），以确保游戏保持流畅。

### 层矩阵高亮

![层矩阵高亮](media/layers-matrix.gif)

当项目中有很多层时，层矩阵可能变得相当难以阅读。这就是为什么在 Flax 1.11 中，我们为鼠标当前悬停的复选框的行和列添加了高亮。

### 线程改进

<center>
<video src="media/falling-boxes.mp4" autoplay muted loop width="100%"></video>
<em>10,000 个下落盒子</em>
</center><br>


我们改进了许多引擎部分，使其更频繁地使用多线程。特别是，物理模拟结果处理、内容流式处理、粒子等等。在示例下落盒子项目中，当所有 `10,000` 个盒子碰撞时，最低 FPS 从平均 `47fps` 上升到 `75fps`。这显示了**引擎在大型项目中扩展得更好**，其中对象数量呈指数级增长以满足视觉保真度。

### GPU 纹理预览

<center>
<video src="media/gputexture.mp4" autoplay muted loop height="200"></video>
</center><br>


当将 `GPUTexture` 暴露给属性面板时，只要有东西渲染到它，它现在将显示预览。

### UI 编辑器分辨率预览

![UI 编辑器分辨率预览](media/ui-resolution-preset.png)

你现在可以在**小部件编辑器中以不同分辨率预览你制作的 UI**。这对移动开发特别有用。它还允许你添加自定义分辨率作为预设。

### 预制体差异改进

![预制体差异改进](media/prefab-diff.png)

**预制体更改视图现在显示添加和移除的 Actor**。你也可以通过 *Apply All* 按钮将更改应用到预制体，就像你在场景编辑器中对预制体进行的任何其他更改一样。

### 轮式车辆改进

![轮式车辆改进](media/vehicle.png)

Flax 现在可以**显示轮式车辆每个车轮的名称和索引**。这使得分配和调试车轮及其碰撞体变得更加容易。

还有新的方法来访问手刹、刹车和转向值。

## 迁移指南

### 模板缓冲区

默认深度缓冲区格式已从 `32-bit` 仅深度更改为 `24-bit` 深度和 `8-bit` 模板。现在，模板包含逐像素位掩码，其中前 5 位用于存储对象层值（0-31）。最后 3 位目前未使用（可由游戏使用）。此更改可能会降低深度质量——调整摄像机近/远平面以解决任何 Z-fighting 伪影。

### Lambertian 漫反射更改

漫反射阴影已修复，不再在光照方程中偏向 `NoL`（`N` 向量点乘 `L`），以绘制更准确的阴影。如有需要，调整阴影偏差以补偿阴影精度伪影。

### `ISceneRenderingListener` 异步

`ISceneRenderingListener` 事件不再受互斥锁保护。现在，它们可以在任何线程上的任何时间被调用，因此锁定你的数据以实现正确的访问（例如，当资源流式传输更新场景对象时）。此更改提高了全局 SDF 和 DDGI 在流式传输大型世界时的引擎性能。

### `CreateAndSetVirtualMaterialInstance` 更改

[Material Instance](../../graphics/materials/instanced-materials/index.md) 初始参数已更改，默认情况下不覆盖基础值，除非通过值设置器显式指定。这改善了复杂项目中的工作流程，并有助于从代码动态创建的实例，这些实例默认应继承基础参数并仅覆盖特定的参数。

### 平台更新

我们更新了各种平台的最低版本，以支持最新的功能和稳定性：
* Windows - 版本 `10`,
  * 最低 CPU 架构要求是 `SSE4.2`,
  * 根据 [Steam 硬件和软件调查](https://store.steampowered.com/hwsurvey/)，PC 上的支持率为 99.78%，
* macOS - 版本 `13`,
* iOS - 版本 `15`,
* Linux - Clang 版本 `14`（在 Ubuntu 24 LTS 上测试），
* Switch - 版本 `20.5`。

## 更新日志

### 版本 1.11.6802.0 - 2025年10月29日

贡献者：mafiesto4、GoaLitiuM、xxSeys1、Tryibion、zetcom89、Weyzu、alsed、fibref、will11600、Zode、Swiggies、GasimoCodes、IceReaper、MineBill、ZaPx64

合并的 PR：129

* 添加了 **新的内存性能分析器**
* 添加了对 Mac 上 Tracy 性能分析器的支持
* 添加了与 Tracy 性能分析器的集成，以绘制主要内存类别
* 添加了新的 Arena 分配器，用于具有共享生命周期的优化动态内存分配
* 添加了虚拟页面的内存分配分析（在 Win32 上）
* 为 Apple 平台添加了进程内存统计信息
* 向游戏设置添加了版本
* 在调试视图中基于级联添加了可变 DDGI 探针大小
* 向 Tracy 集成添加了 **GPU 性能分析支持**
* 添加了 Nsight Graphics 的调试工具检测
* 添加了显式 GPU 资源转换、内存和计算屏障
* 在 Vulkan 上添加了记录缺失实例层
* 在 D3D12/Vulkan 上为 PSO 捕获添加了调试名称，包括 Development 构建
* 添加了 `Platform::Yield`
* 添加了 `PLATFORM_CONSOLE` 定义，并将桌面修复为 Windows/UWP，而不是 Win32
* 添加了在绑定到 `Function` 的 lambda 中使用自定义内存分配器的支持
* 添加了 `GPUResourceMapMode.NoWait` 标志以控制缓冲区数据读取
* 添加了 `nvapi` 第三方模块（NVIDIA 驱动程序扩展）
* 添加了 `AGS` 第三方模块（AMD 驱动程序扩展）
* 向 D3D11 添加了 `nvapi`/`AGS` 库，以在 NVIDIA/AMD GPU 上实现高效的 UAV 写入重叠
* 添加了自动定期 Vulkan 管线状态缓存序列化
* 向 Tracy 性能分析器区域添加了红色，以显示导致 CPU 等待的区域，从而改进性能分析
* 向异步场景加载的反序列化阶段添加了时间切片，以避免卡顿
* 在帧中基于空闲时间添加了更高级别的流式传输时间预算
* 向引擎配置添加了 `UseLogInRelease`，以在 Release 构建中禁用日志记录
* 在材质中添加了用于自定义光照的 **新的 Custom Lit 着色模型**（例如 **Cel Shading**）
* 向材质中的自定义代码节点添加了调整大小功能
* 改进了 ColorValueBoxes 绘制透明颜色的方式
* 添加了在没有日志记录的情况下构建引擎的支持
* 添加了标签/按钮文本到字符串的转换，以便更好地调试
* 添加了 `CharacterController.Resize`，用于快速实现角色蹲伏
* 添加了更改角色控制器胶囊体原点以从脚部位置开始的选项
* 添加了 `SCOPE_EXIT` 宏
* 向模型导入选项添加了 `PositionFormat` 和 `TexCoordFormat`，用于网格数据编码
* 添加了 `GPUDevice.VideoOutputs`，包含连接的显示器列表
* 添加了带有对象层信息的模板缓冲区使用
* 添加了新的场景纹理 `SceneStencil` 和 `ObjectLayer`，用于采样模板缓冲区
* 向贴花添加了 **渲染层遮罩**
* 向平台 API 添加了 `ReadWriteLock`
* 在 Android 上添加了对大型 16kb 页面大小的支持
* 添加了 Android 原生崩溃缓存
* 添加了新的 `PreIntegratedGF`，内存减少 80%，但数据更准确
* 在材质生成时，添加了对材质特性模板源的热重载支持
* 向构建设置添加了并使用 `Max Mesh Position Error`，用于自动网格顶点位置存储
* 改进了导入路径 UI
* 为编辑器添加了带有 `IBrush` 的 `LayoutElementsContainer.Image`
* 添加了新的 `UIBrush`，它使用带有画笔数据的 `UIBrushAsset` json 资源
* 添加了快速聚焦调试控制台输入的热键（`Ctrl+~`）
* 向层矩阵编辑器添加了高亮，以改善用户体验
* 向 Visject 添加了节点对齐格式化选项
* 向带有上下文菜单的 Visject 插座添加了高亮，并改进了按钮文本
* 向 Visject 曲面添加了加法（框）和减法选择（使用 Ctrl 添加到选择或使用 Shift 从选择中移除）
* 在编辑器中为视图标志和调试视图模式添加了快捷键
* 在编辑器中为窗口添加了快捷键
* 向内容查找器工具添加了强调色
* 添加了使用方向键换行滚动项目列表，并简化了滚动逻辑
* 为 `MainMenu` 添加了 `GetOrAddButton` 工具
* 添加了 `GPUTexture.UploadData`，用于通过 `TextureData` 容器更改纹理内容
* 向粒子添加了 `Texture Size` 节点
* 为粒子发射器添加了调试形状绘制的选项
* 添加了在碰撞体数据窗口中显示碰撞模型的顶点和三角形的选项
* 向材质时间节点添加了未缩放时间
* 添加了移动 Visject 连接的支持
* 添加了 **天空盒旋转功能**
* 添加了从模型预制体自动恢复模型导入选项
* 添加了更多颜色渐变节点停止点（从 8 个增加到 12 个）
* 添加了粒子数据布局调试器选项卡
* 添加了用于禁用变换 Gizmo 亮度的编辑器选项
* 从预制体差异菜单中添加了显示添加和移除的 Actor
* 通过上下文菜单添加了集合项复制
* 添加了编辑器选项，用于根据最长文本自动调整属性列表拆分器条的大小
* 添加了自定义编辑器启动画面图像（在项目 `Content` 文件夹中导入纹理 `SplashImage`）
* 添加了编辑器设置的快捷键
* 向 Actor 工具箱搜索添加了控件和过滤器
* 在添加碰撞体菜单中添加了添加胶囊体的选项
* 为窗口和选项卡的行为/逻辑添加了更多编辑器选项
* 向 Animated Model 添加了新的 API，以更好地控制自定义节点
* 向音频源编辑器添加了带有时间位置的滑块
* 向音频源添加了 `ClipStarted` 和 `ClipFinished` 事件
* 添加了从编辑器内部创建新项目的选项
* 添加了在内部解析带有预处理块的继承的支持
* 添加了对 Visual Studio 2026 和 v145 MSVC 工具集的支持
* 添加了绘制刚体质心和附加的碰撞体
* 在调试日志暂停时添加了信息消息
* 添加了一个断链图标（用于 Actor 缩放编辑器）
* 添加了在颜色属性中显示透明颜色的更好方式
* 向 C++ 脚本 API 添加了 `InvokeOnUpdate`
* 为 Layer Mask API 添加了更多工具方法
* 添加了 **nuget 包支持** 的初步工作
* 为编辑器中的树节点添加了 shift 选择
* 将 VSCode 组织为将项目插件放入自己的文件夹
* 将最大支持的 .NET SDK 版本更新到 10
* 添加了预制体 UI 视口缩放
* 添加了曲面格式化选项，以拉直节点连接
* 添加了可搜索的可视化脚本编辑器方法覆盖上下文菜单
* 添加了在节点上使用 RMB 平移 Visject 编辑器
* 为视口图标添加了选项
* 添加了在使用编辑器上下文菜单时防止自动保存
* 添加了输出窗口控制台，以在空格键上显示所有命令
* 添加了在使用烘焙并运行时用于构建配置的编辑器选项
* 添加了使用来自链接场景渲染的阴影贴图的支持（例如，用于 1p 武器）
* 添加了着色器头代理，以便在编辑器内轻松创建 `.hlsl` 文件
* 添加了对雾的负 Fog Cutoff Distance 的支持，以在摄像机远平面之前绘制它，无论设置如何。
* 向预制体差异视图添加了 Actor 名称
* 向调试窗口组按钮添加了快速切换
* 添加了使用 `Shift` 键快速展开标签编辑器
* 添加了在编辑器下拉列表中用键盘滚动时展开折叠类别的能力
* 改进了内容查找器的键盘导航，并修复了右键单击清除搜索的问题
* 添加了快速跳转到包含它的本地化字符串表的按钮
* 添加了相对于距离的视口图标缩放和编辑器对其的控制
* 添加了用于运行模式中的实用程序，该程序为已发布的应用程序返回 true（`Engine::IsPlayMode`）
* 如果未选择任何内容且用户按下回车键，添加了自动选择项目列表上下文菜单中的第一个项目
* 在工具中添加了更好的不受支持的 .NET 版本记录
* 添加了导入/导出警告消息框
* 向环境探针添加了 `SortOrder`
* 在 iOS/macOS 上添加了异常和崩溃捕获及处理
* 优化了资源引用以支持直接注册，从而减少 `Delegate` 内存分配和开销
* 优化了捕获数据时编辑器性能分析器的分配
* 优化了 `SceneRendering` 中的 Actor 注册，以跟踪空闲项目
* 优化了异步场景加载，以在具有时间切片的单独阶段中运行
* 优化了物理模拟，将 `8` 个线程的限制提高到更高
* 通过作业系统在异步中优化了物理变换更新传播
* 改进了 Actor 渲染条目的更新，使用更好的线程锁定，支持 Actor 更新时的异步写入
* 优化了 `Actor::DestroyChildren`
* 优化了 `ContainerControl.DisposeChildren`
* 优化了 CSharp 脚本运行时，为每个程序集使用 arena 分配器
* 使用新的 Arena 分配优化了托管运行时互操作集合的动态内存分配
* 优化了 `ProbesRenderer`，对立方体贴图面渲染和过滤使用时间切片
* 优化了 `SpriteRenderer` 延迟初始化
* 优化了粒子绘制，使用整个 `RenderContextBatch`
* 优化了 GPU 粒子模拟，在调度之前执行内存缓冲区复制/更新
* 优化了 GPU 粒子 Bitonic 排序，使用单独的缓冲区和键缓冲区，以避免额外的缓冲区复制
* 优化了 GPU 粒子模拟、排序和绘制，使用了更好的资源转换屏障
* 通过首先调度较大的模拟以重叠更多工作，优化了 GPU 粒子模拟
* 当不变时，优化了 D3D12 和 Vulkan 上的计算着色器管线绑定
* 优化了小型发射器的排序粒子索引格式为 16 位
* 优化了环境探针过滤着色器
* 使用并发 arena 分配器优化了植被渲染
* 使用页面分配器优化了 Vulkan 上的纹理/缓冲区上传
* 使用批处理内存屏障优化了 GPU 纹理和缓冲区上传
* 使用 UAV 写入重叠优化了全局 SDF 调度
* 优化了 DDGI 探针更新，同时重叠辐照度和距离数据调度
* 优化了样条线的调试绘制性能，使用视锥剔除
* 优化了调试命令名称构建，并支持具有名称层级的嵌套类
* 优化了已烘焙构建中的 `Asset::GetPath`
* 优化了远离摄像机的样条线调试渲染
* 优化了粒子绘制，在异步作业中运行
* 优化了 AOT 平台上的脚本对象生成
* 优化了音频流式传输，以减少锁争用
* 如果与前一帧相比未更改，优化了颜色分级 LUT 缓存
* 优化了文本渲染相关函数中的内存分配
* 优化了物理碰撞体视图模式中地形形状的调试绘制
* 优化了具有大量树节点的缓慢树展开/折叠
* 使用深度测试优化了贴花渲染
* 优化了 Animated Model 初始化时的内存分配
* 优化了布料读取动画模型姿态时的内存分配
* 优化了更新大量图块时的导航网格构建器
* 优化了 PhysX 工作调度器，使其由所有场景共享
* 将 Tracy 更新到 `v0.12`
* 将 OpenAL 更新到 `1.24.3`
* 将 `volk` 更新到版本 `1.4.304`
* 更新到最新的 Nintendo Switch SDK 版本 `20.5`
* 将最低 Windows 版本更新到 `10`（以匹配 .NET 8）
* 将 Windows 上的最低 CPU 架构要求更新到 SSE4.2
* 将最低 macOS 版本更新到 `13`，iOS 更新到 `15`
* 将 Linux 的最低 Clang 版本更新到 `14`
* 重构了材质实例，使其默认不覆盖所有公共参数（需要显式设置）
* 重构了关卡操作，以支持时间预算和时间切片
* 重构了颜色分级 LUT 渲染，以支持 2D/3D 模式配置
* 重构了高光光照，以将高光正确映射为 BRDF 中的反射率
* 将 `Prefetch` 重命名为 `MemoryPrefetch`
* 移除了 `ConcurrentSystemLocker`，改用 `ReadWriteLock` 以实现更好的线程同步
* 修复了顺序网格缓冲区更新上的内存泄漏
* 修复了 `AnimatedModel` 上缺少的 `BoundsScale` 使用
* 修复了编辑器中 UI 元素之间的各种边距和间距不一致
* 修复了性能分析器内存传播（在事件层级中）
* 如果启用了大世界，修复了摄像机视图矩阵计算以使用双向量类型
* 修复了输出日志控制台从历史记录中移除已执行命令，无论位置如何
* 修复了输出日志控制台调整宽度以包含整个项目文本
* 修复了样条线缺少的球体边界更新
* 修复了在其他脚本类内部嵌套类的支持
* 修复了 `ContainerControl` 上的 `AutoFocus` 默认为 `false`
* 修复了在编辑器中隐藏聚焦控件以保持游戏手柄导航时游戏 UI 焦点丢失的问题
* 修复了更新文本渲染布局时材质实例上的内存泄漏
* 修复了绘制调用批处理在不透明通道中忽略距离，以实现更好的绘制调用实例化
* 修复了由于体积雾导致的反射探针捕获在立方体贴图面边缘的接缝
* 修复了已烘焙构建中调试名称中的着色器资源路径
* 修复了 `Asset.WaitForLoaded` 在某些平台上产生或休眠线程以进行正确的多线程处理
* 修复了 `MClass::GetNamespace` 返回名称而非命名空间的拼写错误
* 修复了 .NET 版本使用固定设置的控制台选择
* 修复了 AOT 构建中缺少的 `TypeConverter` 支持
* 修复了修改 corlib 时 AOT 库重建以避免版本不匹配
* 修复了游戏启动画面等待纹理流式传输完成，而不仅仅是分配
* 修复了 `GPUBufferDX11::Map` 在读取暂存缓冲区时等待数据以避免丢失数据
* 修复了 PSO 初始化在重新创建时释放旧数据
* 修复了性能分析器表使用居中对齐的列标题
* 修复了 Vulkan 描述符集池
* 修复了调试命令类型检测与参数一起使用时的问题
* 修复了 `HtmlParser` 在解析带有不正确标签的文本时的死循环
* 修复了 `ManagedDictionary` 缓存在热重载时被清除
* 修复了 `ConcurrentSystemLocker` 一次只允许一个写入者
* 修复了渲染输出控件中不正确的图像缓冲区使用
* 修复了 macOS 上的全屏无边框窗口
* 修复了 `CustomEditorWindow` 使用的错误
* 修复了拖动项目时滚动条不更新
* 修复了当编辑 GUI 选项为 false 且游戏正在播放时的 GUI 输入
* 修复了拥挤的颜色分级编辑器值框
* 修复了编辑器中树节点绘制
* 修复了尝试将场景添加到 Actor 组
* 修复了 `atan2` 节点使用默认框值
* 修复了已弃用的资源保存跳过临时或虚拟资源
* 修复了无效粒子数据上的崩溃为软检查
* 修复了如果资源无法创建，新资源文件创建对话框按钮
* 修复了新创建的骨骼遮罩能够打开
* 修复了 json guid 解析检查正确的十六进制字符
* 修复了 `Task::Wait` 中的超时计算
* 修复了加载没有网格的模型
* 修复了粒子发射器等待使用的纹理加载并流式传输，以便在图中正确使用
* 修复了从模式自动碰撞导入移除未使用的材质槽
* 修复了使用延迟着色的材质时的透明度相关材质选项
* 修复了动画预览边界缩放
* 修复了场景光照贴图移除清除 Actor 到光照贴图的链接
* 修复了编辑器中本地化字符串缺少默认值
* 修复了清空搜索框时滚动到视图中的选定 Actor
* 修复了在 Visject 曲面编辑器中放大和缩小时的行为
* 修复了 Visject 曲面选择与 Control 切换节点的选择
* 修复了节点删除按钮未检查用户是否在曲面上执行某些操作
* 修复了在编辑器中聚焦具有缺失边界的自定义 Actor
* 修复了粒子发射器编辑器源代码按钮自行启用/禁用
* 修复了更改选定选项卡后执行布局
* 修复了在预制体编辑器中滚动到选定节点
* 修复了在选择资源选择器资源时清除内容窗口搜索
* 修复了在播放开始或结束时跳过场景树滚动到节点
* 如果控件在布局期间发生更改，修复了面板滚动条更新再次执行
* 修复了编辑器中网格引用上的预制体差异
* 修复了预制体实例根名称从不从场景实例应用
* 如果调试绘制被禁用，修复了缺少的物理碰撞体视图
* 修复了雾通过平面绘制 Fog Cutoff Distance，而不是球体测试
* 修复了在运行时更改 CharacterController 中心以保持 Actor 放置
* 修复了复制 json 资源以正确重新映射对象 ID
* 修复了场景重载时工具栏不更新的问题
* 修复了在大世界中移动摄像机时地形碰撞调试绘制闪烁
* 修复了使用位置偏移时的地形着色器错误
* 修复了向量和四元数相等检查使用精确的分量值
* 修复了使用 beta 构建时的 Android NDK 版本解析
* 修复了 `BitArray` 位索引
* 修复了 `Array::RemoveAt` 移动元素并避免数据复制
* 修复了热重载文件在启动期间未被清理
* 修复了富文本框标签解析中标签未完成时的无限循环
* 修复了循环时最小或最大帧上的动画事件边缘情况
* 修复了反转时动画事件播放
* 修复了直接在材质输入中使用材质 VS 到 PS 节点
* 修复了保存带有混合形状的蒙皮模型
* 修复了地形法线上的轻微伪影
* 修复了在贴花中采样纹理以使用自定义 mip 级别
* 修复了低阴影质量上的阴影偏差伪影
* 修复了大世界原点更改上的运动向量稳定性
* 修复了 `flat normals` 外观上的 SSAO 伪影
* 修复了 SSR 不需要时跳过运动向量渲染
* 修复了构建导航网格图块时 recast 对象上的内存泄漏
* 修复了缺少/不正确的工具链异常仅记录一次
* 修复了物理碰撞体视图模式中错误渲染的透明度
* 修复了未将更新的碰撞数据附加到刚体
* 修复了 `CheckBox` 在没有编辑器图标图集的游戏中创建时显示选中状态
* 修复了 `HashSet::Add` 返回不正确的值
* 修复了在嵌套预制体中恢复预制体对象引用
* 修复了在不可调整大小的集合编辑器上复制和移除选项可点击
* 修复了在预制体中编辑 UI 画笔
* 修复了预制体窗口为空的 UI 控件预制体使用 UI 模式
* 修复了应用子对象的本地更改时预制体根名称和变换被更改
* 修复了嵌套预制体内部的对象 ID 重新映射
* 修复了在预制体视口中调试绘制轮式车辆
* 修复了不正确的 Lambert 漫反射阴影，使用 `N dot L` 以获得准确光照
* 修复了可能在同一网格上多次应用的节点上的模型导入缩放
* 修复了 Blend Poses 节点在过渡返回时平滑混合回来
* 修复了内容窗口导航栏扩展工具栏以正确显示滚动条
* 修复了输出窗口控制台调整弹出窗口大小
* 修复了在返回不同对象的 get 集合属性上的编辑器撤销
* 修复了脚本字段预制体差异显示和撤销恢复
* 修复了从非根预制体克隆 Actor 时的警告
* 修复了 C++ 中场景对象指针序列化使用正确的序列化器
* 修复了 TextRender 的缩放
* 修复了富文本框垂直对齐
* 修复了摄像机剪切时体积雾闪烁
* 修复了影响透明材质的缺失体积雾
* 修复了方向完全垂直时的太阳阴影
* 修复了视距光属性一直影响亮度的问题
* 修复了使用 Assimp 导入材质的自发光、粗糙度、金属感和线框属性
* 修复了地形导出以正确计算大小和采样位置
* 修复了当发射器更改并在树中激活时粒子效果参数应用覆盖
* 修复了使用位置偏移时的粒子材质编译错误
* 修复了正交和斜投影中的天空渲染
* 修复了使用输入骨骼姿态时缺少根运动复制（例如 IK 节点）
* 修复了加载具有与引擎文件夹相似路径的项目
* 修复了禁用地形时结束播放的地形物理错误
* 修复了更改刚体质心偏移
* 修复了复制带有父级的样条线
* 修复了多行文本超过行结束字符后的字体命中测试
* 修复了使用枚举作为场景动画事件参数
* 修复了在太小视口中渲染泛光或镜头光晕时的崩溃
* 修复了内容流式传输处于活动状态时退出引擎的崩溃
* 修复了空缓冲区中无效 GPUBuffer 顶点布局的崩溃
* 修复了内存流读取失败并执行软错误处理时的崩溃
* 修复了由于材质错误，使用具有比基础更多参数的材质实例时的崩溃
* 修复了导入具有不正确三角剖分的网格时的崩溃
* 修复了将模型作为预制体导入并处理重复对象名称时的崩溃
* 修复了检测到着色器图递归中的堆栈溢出异常的崩溃
* 如果资源变为 null，修复了 GPU 内存性能分析器中的崩溃
* 修复了 Vulkan 描述符集数据中内存访问的崩溃
* 修复了粒子排序内存访问的崩溃
* 修复了预制体根对象反序列化失败时的崩溃
* 修复了卸载具有空闲剩余流式传输任务的纹理时的崩溃
* 修复了 Android 上脚本后端的崩溃

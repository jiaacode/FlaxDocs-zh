# Flax 0.7 发布说明

## 亮点

### 新项目格式

![新项目格式](media/project-structure.png)

在发布 0.7 更新之前，最重要的变化之一是对 Flax 项目格式和布局进行现代化改造，以确保它们能够在未来扩展。旧的 *Project.xml* 描述符被替换为 **&lt;project_name&gt;.flaxproj** 文件，该文件采用 Json 格式，包含所有基本项目信息以及 **项目引用**，这使得可以在游戏中使用外部插件项目。我们还添加了[脚本模块](../../scripting/index.md)，因此你的游戏代码可以是模块化的（类似于 Flax 引擎代码库）。

Flax 编辑器将 **自动升级旧项目**，并在任何操作之前要求进行 **安全备份**，因此你所有数据都将安全。

如果你下载新的 Flax 启动器或更新已有的启动器，它将允许你通过双击打开 Flax 项目文件，并使用上下文菜单生成脚本项目文件。

### C++ 脚本

![C++ 脚本](media/cpp-scripting.png)

它终于实现了！你可以在 Flax 游戏中使用 **C++** 来编写游戏逻辑脚本。从本次更新开始，我们将分发引擎头文件和构建工具，用于在 Flax 中编译和运行原生代码。你所需要的只是安装带有编译器的平台相关工具集，然后就可以开始了！

Flax 支持 **无缝的 C++ 和 C# 脚本**。我们创建了一个开放生态系统，你可以在其中混合使用两种语言，并利用 Flax 工具来创建出色的项目。我们成功地在 [Flax.Build](../../editor/flax-build/index.md) 中编写了 **自动 C# 绑定生成器**，创建了 **用于虚调用的原生对象虚表劫持**，并支持在编辑器中 **热重载 C++ 和 C#**。

要了解更多信息，请参阅官方文档[此处](../../scripting/cpp/index.md)。

新的 C++ 脚本支持需要对现有的游戏构建管线进行现代化改造。因此，构建日志得到了改进，构建过程具有更多可自定义的事件，并且支持自定义文件部署。着色器也可以使用额外的调试数据或不进行优化进行烘焙，以便在目标设备（例如 PS4 DevKit）上进行调试。

### PlayStation 4 和 Xbox Scarlett 支持

![Flax 上的 PlayStation 4](media/flax-ps4-gameplay.gif)

每次更新都将 Flax 推向 AAA 级引擎。这次我们将引擎移植到了 **PlayStation 4** 和即将推出的 **Xbox Scarlett**（即 Xbox Series X）。借助 Flax 的强大功能，你可以创造惊人的体验，并轻松将它们发布到多个平台。

Flax 对 PlayStation 4 和 Xbox Scarlett 平台的支持受 NDA 保护。如果你希望在 PS4 上开发游戏，请联系我们（仅限获得 Sony 批准并拥有 PS4 SDK 访问权限的开发者）。

### 使用着色器进行图形编程

![Flax 中的自定义着色器和 GPU API](media/sharpen-filter-example.png)

Flax 已经拥有使用材质图创建着色器的出色工具，但我们希望更进一步。在此更新中，你可以使用我们的低级图形抽象层，该层支持直接在 C++/C# 代码中使用 GPU。你可以创建和使用 GPU 资源，如纹理/缓冲区，并创建图形命令。

另一步是在游戏和插件项目中添加完整的 **HLSL** 着色器支持，因此你可以编写自己的着色器库并在游戏中使用它们。我们随引擎源代码一起发布了 Shader API 库，其中包含许多用于创建精美着色器的工具。你可以编写像素、几何、外壳、顶点和计算着色器来执行任何 GPU 计算。我们公开了低级绘制 API，因此你可以创建自己的几何体并使用自己的着色器进行渲染，或扩展现有的内置渲染。最后，着色器现在支持在编辑器中的自动热重载（当源文件或被包含的任何文件被修改时）。

要开始创建自己的着色器，请参阅我们的文档和教程[此处](../../graphics/shaders/index.md)。

### Gameplay Globals

![Gameplay Globals 运行时](media/gameplay-globals-editing.gif)

在创建依赖于内容的更复杂游戏系统或创建技术美术时，提供可以影响整个游戏玩法的全局参数通常很有用。例如，玩家队伍颜色、风向、风速或其他天气参数。**Gameplay Globals** 资源包含一个命名参数列表，可以在项目中全局访问，包括材质、动画和粒子。它们的值可以由美术师在编辑器中设置，或从代码驱动。这极大地提高了生产效率，并为游戏开发者提供了一个丰富其游戏内容的绝佳工具。

![资源中的 Gameplay Globals](media/gameplay-globals-4.png)

### UI 锚点和偏移

![UI 锚点和偏移](media/anchor-presets.png)

我们对 UI 定位和锚点系统进行了现代化改造，使其更加灵活。控件现在具有锚点最小值和锚点最大值属性，可以手动设置或通过 *锚点预设* 编辑器设置。控件锚点是父控件中左上角和右下角的归一化（范围为 0-1）位置。这可用于将控件停靠在父容器内，使控件的位置/大小可以依赖于父容器的边界。

### 材质、粒子图和动画图函数

![材质函数](media/material-function.png)

在创建复杂材质、动画图或粒子系统时，通常需要多次重用图的相同部分。例如，三平面映射计算可以在多个地形着色器中重用，而无需复制粘贴。为了实现这一点，我们创建了一组新的资源：**材质函数**、**动画图函数** 和 **粒子图函数**。它们可以包含一组自定义的输入和输出，以及实现特定逻辑的图。然后，你可以在许多其他资源中重用该函数，Flax 将自动处理重载和实时内容编辑，使你的游戏开发过程更加顺畅。

从这里[此处](../../graphics/materials/material-functions.md)开始是一个关于材质函数的教程。

### Blend Shapes 支持

Flax 支持为蒙皮模型导入和使用 Blend Shapes（即变形目标）。这对于在对话场景中实现角色面部表情或口型同步非常有用。导入资源时，请勾选 **Import Blend Shapes** 选项。然后你将能够在蒙皮模型窗口中预览 Blend Shapes（每个通道都可以在 Blend Shapes 组下调整）。然后，你可以在运行时使用 [AnimatedModel](https://docs.flaxengine.com/api/FlaxEngine.AnimatedModel.html) 类的方法 *SetBlendShapeWeight* / *GetBlendShapeWeight* / *ClearBlendShapeWeights* 从代码控制 Blend Shapes 的外观。

### 反向动力学

![Two Bone IK](media/two-bone-ik.gif)

此更新包含新的动画图节点：**Two Bone IK**，它在三个节点的链上执行反向动力学（例如玩家用手拾取枪支），以及 **Aim IK**，它旋转一个节点使其瞄准目标（例如用于玩家头部注视）。我们还改进了图中骨骼节点的变换，因此它运行得更快且更稳定。

### 自动模型 LOD

![自动模型 LOD](media/automatic-model-lod.gif)

另一个令人兴奋的功能是在源资源导入期间自动生成模型 LOD。在导入模型文件时，只需勾选 **Generate LODs** 属性（在 *Level Of Detail* 组下）并调整设置。导入的模型将包含简化的 LOD，这将优雅地提高游戏的性能。此选项也适用于蒙皮模型。Flax 还将为切换 LOD 选择更好的屏幕尺寸，以便玩家不会注意到 LOD 过渡（材质中的 *Dithered LOD Transition* 选项有助于隐藏突现伪影）。此外，较低的 LOD 可以使用不同的材质——例如，使用更少纹理的更优化材质，因此你可以开发性能更高的游戏。
我们使用：<https://github.com/zeux/meshoptimizer> 库进行网格简化和生成几何体的优化。导入此模型的部分日志：

```
Generated LOD1: triangles: 7100 (48% of base LOD), verteces: 3052 (16% of base LOD)
Generated LOD2: triangles: 3352 (22% of base LOD), verteces: 1153 (6% of base LOD)
Generated LOD3: triangles: 1516 (10% of base LOD), verteces: 484 (2% of base LOD)
Generated LODs for 3 meshes in 17 ms
```

### 更多编辑器功能

![编辑器中的模型纹理坐标预览](media/model-uv-preview.gif)

每次更新都为 Flax 编辑器带来更多稳定性、性能和闪亮的功能。最有趣的有：
* **自定义编辑器主题** 支持（由 *stefnotch* 制作）
* 模型纹理坐标、光照贴图 UV 预览
* 几何体顶点颜色和光照贴图 UV 密度调试视图
* 用于生成自定义 Actor 的搜索选项卡
* 在编辑器中打开着色器资源
* 带有资源信息的模型/纹理工具提示
* 从模型 **自动导入材质和纹理**
* 内容窗口中的视图选项

![自定义编辑器主题](media/editor-custom-theme.png)

### 最后的重构

最后，我们修改了 C# 脚本 API（FlaxAPI 仓库将不再接收新源）。这与我们使用的新自动绑定生成器和主要项目重构有关。从本次更新开始，我们已开始为第一个稳定版本 1.0 做准备，因此我们正在等待你的反馈（尤其是在新的 C++ 脚本方面）。Beta 版本结束后，API 更改将不会在次要更新之间破坏现有代码，任何旧代码都将被标记为已弃用。此外，我们将至少保留一年的旧资源自动转换代码，以确保没有人丢失其数据。如果在升级现有 Flax 项目时遇到问题，请告知我们。最后，我们更新了 [Flax Samples](https://github.com/FlaxEngine/FlaxSamples)，因此你可以查看。

## 更新日志

### 版本 0.7.6196 - 2020年7月13日

* 添加了 **对 Visual Studio Code 的支持**
* 向 `CollectionAttribute` 添加了 `Spacing` 功能
* 在编辑器 UI 中的材质槽、输入操作/轴和模型条目之间添加了间距
* 在 Windows 上的 Flax.Build 和编辑器中添加了 VS Code 检测
* 添加了对自动将新脚本添加到 Visual Studio 项目的支持
* 添加了使用 shift+鼠标滚轮更改编辑器摄像机速度
* 向编辑器选项添加了 *Interface Scale* 选项
* 在编辑器内运行时自动聚焦到游戏 UI 根控件（如果选中）
* 添加了对 Visject 参数特性的支持
* 在编辑器的动画图模型参数列表中添加了动画图参数特性
* 在材质实例界面中添加了材质参数特性
* 在粒子系统窗口和粒子特效属性面板中添加了粒子发射器图参数特性的使用
* 在 ObjectSwitcherEditor 编辑器中添加了展开 Value 组
* 添加了对显示模拟中当前 GPU 粒子数量的支持
* 添加了对 Flax.Build 整个参数引用的支持
* 为 Gameplay Globals 资源添加了拖放到图曲面的功能
* 增加了输出日志窗口中滚动条的大小
* 增加了默认场景光照亮度以获得更好的外观
* 在重命名项目时禁用了内容窗口中的滚动
* 优化了 Visject 图回调的堆栈跟踪
* 优化了着色器图和 Visject 图的代码库
* 修复了在内容窗口中滚动到项目进行重命名
* 修复了使用字典查找时 MAssembly::GetClass 上的内存分配
* 修复了 RichTextBoxBase 中单个文本块的剔除
* 修复了 SliderControl 与鼠标的使用
* 修复了如果使用命名空间，骨骼节点名称的导入
* 修复了对 C# 7.2 的支持
* 修复了在 Flax.Build 中检查 Windows 工具链时可能出现的异常
* 修复了如果游戏脚本仅为 C#，则跳过包含 C++ Flax 项目
* 修复了在 VisjectSurfaceWindow 关闭时，由于重命名属性编辑操作结束导致的撤销异常
* 修复了地形创建
* 修复了在没有项目的情况下运行 Flax.Build

### 版本 0.7.6195 - 2020年7月5日

* 修复了导入带有无效材质的 fbx 模型时的崩溃
* 修复了在未安装 Windows 10 SDK 的情况下构建和使用仅 C# 项目
* 修复了仅 C# 项目的项目文件生成（跳过原生项目和工具链检查）
* 修复了将无效的 UTF-8 字符串转换为 UTF-16 时的崩溃

### 版本 0.7.6194 - 2020年7月3日

贡献者：stefnotch、devjarmo

* 添加了 **新项目格式**
* 添加了脚本的 **模块支持**
* 添加了 Flax.Build 与游戏项目的集成
* 添加了对 C++ 脚本的完全支持（需要引擎源代码）
* 添加了对 **在 C# 中重写 C++ 对象虚函数** 的支持（通过虚表）
* 添加了通过 HideInEditor 特性在编辑器中隐藏脚本拾取的支持
* 添加了对 **C# 自定义 Actor** 的支持
* 向工具箱添加了 `Search` 选项卡，用于使用搜索框生成任何对象
* 向启动画面添加了更多有趣的引用
* 添加了在解决方案缺失时，打开文件时自动生成 Visual Studio 项目文件
* 为导航网格边界 Actor 和后期处理效果体积 Actor 添加了 BoxVolume Actor 类
* 添加了通过 C# 对象构造函数创建脚本对象的支持
* 添加了检查 Visual Studio 是否安装了 Flax.VS（如果缺少扩展，则跳过生成特殊的 C# 项目）
* 添加了 **Xbox Scarlett 支持**（Xbox Series X）- 实验性
* 为 D3D12 添加了新的 DirectX 着色器编译器对 *Shader Model 6* 的支持
* 向构建设置添加了 `ForDistribution` 选项
* 向数学库添加了 Remap
* 添加了自定义 **颜色分级 LUT** 支持
* 添加了从任何类创建通用 json 资源的支持
* 添加了如果游戏未在编辑器中运行，则灰色显示序列指示
* 为停靠窗口导航和关闭添加了可绑定的输入快捷键
* 向 GPUContext 添加了 SetViewportAndScissors
* 添加了更多 GPUBuffer C# API
* 添加了在编辑器中显示着色器源代码
* 添加了从编辑器添加新着色器文件的支持
* 添加了在游戏/插件项目中使用 **着色器** 的支持
* 添加了在未指定项目路径时打开编辑器时的项目文件对话框
* 为纹理/模型项目添加了带有元数据的工具提示
* 向停靠窗口上下文菜单添加了最大化/恢复选项（也可通过双击）
* 添加了如果不使用 CSG，则跳过添加 CSG Actor
* 添加了场景构建功能，可一次性构建所有场景数据
* 向 C# 脚本 API 添加了 **GPUShader**
* 向编辑器中的模型窗口添加了模型 UV 和光照贴图 UV 预览
* 向脚本添加了用于自定义渲染的 Draw 事件
* 添加了 `RenderToTextureControl`
* 添加了从模型文件自动导入材质和纹理的支持
* 添加了使用指定项目文件路径而不是目录打开项目的支持
* 添加了 `LightmapUVsDensity` 调试视图
* 添加了 `VertexColors` 调试视图
* 在模型光照贴图图表中添加了基于面积权重的坐标空间放置和矩形打包，为更大的网格分配更多空间
* 向 C# API 添加了低级 GPU 编程
* 在代码编辑器中添加了打开着色器源代码
* 添加了收集原生代码崩溃的支持（堆栈跟踪、内存转储、日志）
* 添加了 `-GenProjectFiles` 命令行选项
* 添加了重置视图标志的选项
* 添加了在后台生成脚本项目文件
* 添加了在加载场景时记录缺失对象的父 Actor 名称
* 在编辑器内容工作区中添加了对 C++ 脚本的支持
* 在编辑器中添加了对运行时热重载 C++ 游戏模块的支持
* 在内容窗口中始终显示脚本项目的扩展名
* 添加了 **PlayStation 4 支持**
* 添加了基于包含文件的着色器自动热重载
* 添加了游戏构建失败时输出日志窗口自动聚焦
* 添加了当包含文件被修改时，为游戏烘焙自动编译着色器的支持
* 添加了 **Gameplay Globals**
* 为 UI 枚举添加了工具提示
* 添加了 Visject 表面参数重命名验证
* 添加了对字典撤销记录的支持
* 向 UI 控件添加了 **锚点和偏移**
* 向内容窗口添加了视图选项（文件扩展名显示、视图缩放字段、项目过滤选择和视图列表模式）
* 为内容项目添加了工具提示支持
* 向构建设置添加了 NoOptimizeShaders 选项
* 添加了 ShadersGenerateDebugData 构建选项，为着色器生成调试信息
* 添加了 **自定义编辑器主题** 支持
* 添加了更多启动画面引用
* 在 Visject 中为选中的节点连接添加了高亮
* 在 Visject 中优先显示节点搜索匹配，其中文本以你正在搜索的文本开头（例如，防止 sin 在 sine 之前显示 asin 和 cosine）
* 在 Visject 中输入时，将已生成节点的上下文菜单带入视野
* 向 Platform 添加了 CanOpenUrl 和 OpenUrl
* 向 Platform 添加了 NetworkConnectionType
* 添加了在无效材质参数类型设置时显示错误而不是崩溃
* 添加了在编辑时 **覆盖粒子发射器参数** 的支持（带有值预览）
* 添加了 **新的 C# 绑定生成器**
* 添加了如果纹理大小未更改，则防止调整纹理大小的检查
* 在引擎和编辑器源代码库中添加了对受 NDA 保护的平台使用的支持
* 添加了 GPUDevice.VideoOutputModes
* 添加了 Content.GetAllAssetsByType
* 添加了 **材质、粒子图和动画图函数**
* 添加了地形和粒子材质预览
* 向构建设置添加了 AdditionalAssets 和 AdditionalAssetFolders
* 向曲面图添加了 Platform Switch 节点
* 向模型导入设置添加了 FlipNormals 选项
* 添加了对全景天空盒的支持
* 为静态模型添加了 **自动 LOD 生成**
* 添加了对蒙皮模型 LOD 的支持
* 添加了事件 Content.AssetReloading
* 在模型窗口中添加了显示当前 LOD 及其统计信息的选项
* 添加了对二进制资源中资源依赖项的支持
* 向动画图添加了随机值生成节点
* 添加了 **Blend Shapes 支持**
* 在 CPU 上添加了蒙皮模型数据缓存
* 在性能分析器事件表树中隐藏了过深的节点
* 添加了在导入时自动检测 hdr 纹理以默认选择合适格式
* 重构了游戏构建配置选项
* 重构了引擎服务
* 重构了着色器包含
* 重构了包含编辑的着色器缓存失效并改进了热重载
* 重构了高级渲染 API
* 将 SceneManager 重命名为 Level
* 将 Keys 重命名为 KeyboardKeys
* 将 ProfilingTools 从 Editor 模块移至 Engine（在 Debug 和 Development 构建中可用）
* 禁用了阴影贴图渲染视图的粒子排序
* 从资源缓存文件中移除了引擎版本后缀
* 移除了为完整 Actor 层级调用 OnDebugDrawSelected
* 移除了在场景树中双击重命名 Actor（使用 `F2` 快捷键或属性窗口或右键单击上下文菜单）
* 移除了在调试日志窗口中显示脚本编译错误/警告（使用输出日志窗口获取更详细信息）
* 从 GameSettings 中移除了 Version（使用项目文件中的值）
* 移除了插件导出器 - 现在插件可以放置在游戏模块引用的插件项目中
* 将属性 MemberCollection 重命名为 Collection
* 重构了 UI 控件系统以支持更灵活的锚点
* 将 GPU 着色器常量缓冲区合并到对象中以减少动态内存分配
* 将默认平面更改为 100x100 并使用正确的轴旋转
* 优化了表面着色器切线帧从顶点传递到像素着色器（使用更少的寄存器）
* 优化了剪裁矩形以匹配视口边界，从而提高渲染代码中的 GPU 性能
* 改进了在使用非常慢的 HDD 时移除文件时的资源文件解锁
* 改进了模型预览的摄像机位置
* 改进了对象属性中字典的编辑
* 改进了导入 fbx 模型时的性能和内存使用
* 改进了调试绘制线框体积
* 改进了拾取线框体积
* 修复了场景渲染任务的摄像机检查
* 修复了 Windows 上的 Alt+F4
* 修复了 sRGB 纹理导入
* 修复了取消停靠后滚动条的使用
* 修复了从 Actor::OnTransformChanged 事件调用 C# 事件
* 修复了通过 Flax 连接模块在 Visual Studio 中打开某些源文件
* 修复了如果资源被外部修改，则丢弃着色器缓存
* 修复了 Windows 7 支持（由于缺少 CreateDXGIFactory2 函数）
* 修复了最大化编辑器工具窗口
* 修复了只读深度缓冲区句柄上的 D3D11 对象泄漏
* 修复了使用世界模拟空间的粒子发射器的绘制调用排序
* 修复了当光照贴图纹理未完全加载时开始烘焙光照贴图时的崩溃
* 修复了文本渲染每次绘制刷新几何体缓冲区
* 修复了方向光阴影贴图通道视图位置
* 修复了从动画图中的嵌套状态机收集资源引用
* 修复了深度缓冲区视图以覆盖完整视距
* 修复了在已烘焙游戏构建中缺失 SkyboxMaterial
* 修复了 RichTextBoxBase 文本命中测试的错误
* 修复了动画模型 LOD 选择的动画更新频率
* 修复了 DirectX 的 ShaderProfile 条目命名
* 修复了在某些情况下的 Windows 文件系统监视器
* 修复了当 Visject 中打开上下文菜单时移动窗口的问题（不再无限缩放）
* 修复了在开始连接后右键单击时 Visject 中显示连接线的问题
* 修复了提取尚未加载的音频片段数据时的崩溃

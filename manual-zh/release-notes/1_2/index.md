# Flax 1.2 发布说明

## 亮点

### 车辆

![Flax 物理车辆](/manual/media/wheeled-vehicle.gif)

此版本增加了对 **车辆** 的支持。现在，你可以使用自定义物理形状、车轮和引擎设置来创建汽车。我们使用 Nvidia PhysX 驱动模拟，并公开了大量高度可配置的属性来调整车辆。它可以用于制作简单的汽车，甚至是高级的逼真赛车游戏。此外，非官方地说，Flax 中的汽车驾驶体验比《赛博朋克 2077》更好。

一如既往，我们准备了完整的文档和教程，介绍如何使用这个新功能[此处](../../physics/vehicles.md)。

### Nintendo Switch 支持

![Nintendo Switch Flax 引擎支持移植](/manual/media/switcher.jpg)

我们很高兴地宣布，我们正在开发 Nintendo Switch 平台支持。很快 Flax 将可供 Switch 开发者使用。一旦准备就绪，我们将通过我们的开发者博客通知你。敬请期待，我们已接近完成。

### 低级网络

![网络示例项目多人游戏](/manual/media/network-sample-menu.png)

在此版本中，我们为多人游戏的 **网络** 功能带来了更多特性。我们实现了一个低级网络系统，其特点是使用网络对等端创建多人会话，并带有网络消息传输层。默认网络驱动程序使用 ENet，并允许使用你想要的任何库（如 ValveSockets、SteamRelay、SteamP2P、Photon Realtime 等）。

与此一起，我们发布了一个开源的[网络示例](../../networking/network-sample.md)项目，以帮助你开始在 Flax 中制作多人游戏。它具有一个游戏服务器-客户端，包含游戏会话、聊天和玩家大厅。我们还有一个简单的[分步教程](../../networking/tutorials/network-client-server.md)，帮助你开始在 Flax 中使用网络。

![网络示例玩家大厅](/manual/media/network-sample-players.jpg)

### 本地化

![Flax 编辑器本地化工具预览](/manual/media/localization-preview.gif)

创建面向全球市场并支持多种语言的游戏可能是一个挑战。在 1.2 中，我们发布了用于游戏内部化的新 **本地化工具**。这包括 UI 语言本地化、值区域设置（数字、货币和日期格式）以及为不同语言创建翻译。我们添加了新的本地化设置，这是一个包含许多工具的仪表板，例如添加新语言区域、在代码或内容中搜索本地化字符串。如上图 GIF 所示，编辑器具有当前语言区域的实时预览选项。

请参阅[本文档](../../editor/localization/index.md)了解更多信息。

### 新编辑器图标

![Flax 编辑器 UI](/manual/media/editor-ui.png)

启动 1.2 更新后，你可能首先注意到的是 Flax 编辑器有了新图标。我们 **提高了图标集的分辨率和样式**，使其更清晰。新的 UI 只是 *感觉更好*。

### 作业系统和任务图

![Flax 引擎性能作业系统任务图](/manual/media/animated-models-5k.gif)

最新的 1.2 更新为大型世界带来了 **巨大的性能提升**。这里是一个示例，展示了 5,000 个动画模型播放动画并以稳定的 60 FPS 渲染（在中档 i5 CPU 上测试）。为了实现这一性能水平，我们创建了一个 **作业系统**，它在每个 CPU 核心上运行一个工作线程，以异步方式执行大量计算。对于游戏系统或引擎系统使用的更高级计算，我们实现了 **任务图**。它可以更好地并行化游戏，并支持异步系统之间的依赖关系。

在此更新期间，我们将所有主要引擎系统转换为异步任务图系统，以确保引擎代码的更高并行化（流式传输、动画、粒子等）。它可以通过你代码中的游戏特定系统进一步扩展。要了解如何操作，请参阅[此教程](../../scripting/advanced/multithreading.md)。作业系统和任务图都可以在 C#、C++ 或可视化脚本中使用。

### Tracy 性能分析器

![Flax 引擎 Tracy 性能分析器](/manual/media/async-particles.png)

为了优化引擎或游戏，开发者需要合适的性能分析工具。内置性能分析器对此有帮助，但对于更高级的优化，我们需要更强大的工具。现在，Flax 支持开源的 [Tracy](https://github.com/wolfpld/tracy) 性能分析器，用于 CPU 仪器化和性能分析。它可以分析游戏和编辑器，支持远程性能分析，并可用于捕获性能分析会话以进行比较。此外，Flax 会自动为 C# 和可视化脚本方法插入性能分析区域，因此游戏性能分析现在更加容易。

为了帮助你开始使用 Tracy，我们创建了专门的[文档页面](../../editor/profiling/tracy.md)。此外，对于 C# 开发者，我们有一个关于 [dotTrace 性能分析器](../../editor/profiling/dot-trace.md) 使用的教程。

### 纹理流式传输

![Flax 引擎纹理流式传输性能质量](/manual/media/textures-streaming.gif)

另一个令人兴奋的功能是 **纹理流式传输配置**。新的流式传输设置资源包含 **纹理组**，用于在运行时管理纹理质量和流式传输选项（每个纹理资源都可以定义其所属的纹理组）。

它可以用于在为移动/Switch 烘焙时剥离纹理分辨率，或在游戏菜单中实现动态纹理质量设置。此外，Flax 将降低一段时间内未被渲染的纹理质量（可配置），以提高游戏性能并减少 GPU 内存使用。此外，我们添加了一个新的 API 来查询当前流式传输统计信息和内容统计信息，可用于实现加载画面，如[此处教程](../../scripting/tutorials/loading-screen.md)所示。

### AMD FidelityFX 超分辨率

![适用于 Flax 引擎的 AMD FidelityFX 超分辨率](/manual/media/amd-fsr-flax.jpg)

[AMD Fidelity FX 超分辨率](https://gpuopen.com/fidelityfx-superresolution/) 是一种尖端的超优化空间升采样技术，可在快速帧速率下产生令人印象深刻的图像质量。我们发布了一个适用于 Flax 引擎的开源插件，实现了 FSR 效果，可以轻松集成到 Flax 游戏中。请访问[此仓库](https://github.com/FlaxEngine/FidelityFX-FSR)了解更多信息。

### 更多功能

![UI 渲染的 9-Slicing 支持](/manual/media/9slicing.gif)

每次更新都会带来许多大型功能，但也包含大量重要的细节。我们继续致力于 **提高质量** 和 **可用性** 编辑器。以下是一些值得注意的列表：

* **UI 的 9-Slicing 支持**（上图 GIF）
* 新的 C++ 文档和教程（请参阅[本节](../../scripting/cpp/index.md)文档）
* 在游戏视口中显示/隐藏 GUI 和 DebugDraw 形状的功能
* 来自 `master` 分支的 **每日源代码构建**（[此处](https://github.com/FlaxEngine/FlaxEngine/actions/workflows/cd.yml) 或通过启动器）
* 空闲时优化的编辑器/游戏 CPU 使用率
* 可视化脚本的 **延迟节点**
* 更快的场景打开速度（总体而言，引擎和编辑器都更快了）
* 带有播放调试（播放/暂停/查找/慢动作）的动画资源预览
* 支持 C# 对象和结构作为可视化脚本或动画图参数
* 大量新的 API，使游戏脚本编写更容易（请参阅更新日志了解更多信息）
* Vulkan 和 D3D12 渲染后端在大型世界中现在更加稳定
* 模块化动画角色支持
* 新的 **时间抗锯齿**，重影更少
* 将默认更新率更改为 60
* 碰撞几何体的调试视图（见下图）

![编辑器物理碰撞预览](/manual/media/editor-collision-preview.png)

## 更新日志

### 版本 1.2.6224 - 2021年8月26日

贡献者：mafiesto4、Erdroy

合并的 PR：0

* 向 C# API 添加了 `Engine.HasGameViewportFocus`
* 向车辆车轮配置添加了 `SuspensionForceOffset`
* 添加了检查以防止 C# 中原生事件的重复释放
* 添加了从场景树窗口选择场景资源的选项
* 向场景上下文菜单添加了卸载选项
* 添加了在运行模式下通过编辑器更改场景的支持
* 改进了时间线 UI 定位，以改善视口导航
* 为场景动画操作 UI 添加了辅助工具提示
* 向时间线视图上下文菜单添加了缩放值框
* 添加了使用鼠标右键平移时间线视图
* 为车辆车轮悬挂配置和状态添加了选项
* 为车辆添加了数字转向选项
* 添加了车辆遥测日志调试选项
* 添加了轮式车辆轮胎选项
* 添加了 `-new` 命令行参数以创建新项目
* 增加了雾属性限制
* 将默认最大窗口宽度限制增加到 8k
* 优化了禁用车辆的更新
* 重构了 Tracy 源位置为静态
* 修复了贴花材质法线向量混合
* 修复了 Actor 的 `MapTrack` 问题
* 修复了粘贴时 Actor 重命名，如果可能则保留先前的名称
* 通过应用悬挂弹簧的压缩来修复汽车车轮位置
* 修复了 WheeledVehicle 驱动和悬挂射线投射
* 修复了悬挂力偏移位置
* 修复了如果无法生成对象（例如类型缺失）时粘贴 Actor 仍粘贴有效对象的问题
* 修复了由于在运行模式启动时选择不为空而导致的罕见崩溃
* 修复了一些内存泄漏
* 修复了 ENet 驱动程序崩溃
* 修复了触发器的无效导航网格构建
* 修复了无效的刚体质心位置
* 修复了编辑后材质参数显示问题
* 修复了上下文菜单键盘箭头导航跳过已禁用项目的问题
* 修复了在项目级别重建布局时包含空项目列表的 UI
* 修复了 C# 中 LocalizedString 序列化的一些问题
* 修复了时间线背景停止绘制以使其更可读
* 修复了吸附到地面使用场景图查询而不是仅物理射线投射
* 修复了移除 Actor 时的窗口焦点问题

### 版本 1.2.6223 - 2021年8月8日

贡献者：mafiesto4、Erdroy、thallard、marynate、blep

合并的 PR：3

* 当使用空网格数据烘焙碰撞时添加警告
* 为 C# 编辑器添加了 `CookingData` 以扩展烘焙过程
* 添加了构建预设和目标名称，以便 Game Cooker 扩展访问
* 添加了更改内容视图中项目顺序的选项
* 添加了在 C# 中创建自定义 BoxVolume 类型的支持
* 为从某些共享自定义类型继承的 Actor 创建场景图节点
* 修复了在没有安装原生平台工具的情况下编译仅 C# 游戏脚本
* 修复了安装 Rider 2021.1 时编辑器启动崩溃
* 修复了 CharacterController 最小尺寸检查，以防止值太小导致崩溃
* 修复了不支持固定 TSC 的旧 CPU 上的启动失败
* 修复了来自 `locale::global` 的潜在运行时错误
* 修复了在预制体窗口中从选择创建预制体
* 修复了执行转换操作时缺失 Actor 子级和脚本的撤销
* 修复了生成继承自抽象 C++ 类的 C# 对象时的崩溃
* 修复了为多网格模型烘焙碰撞数据时的崩溃
* 修复了用于导航网格构建的缺失网格碰撞体顶点变换
* 修复了检测到 2 个 Rider 安装时启动崩溃

### 版本 1.2.6222 - 2021年8月5日

贡献者：mafiesto4、honzapatCZ、GoaLitiuM、W2Wizard、jb-perrier、stefnotch、Crawcik、Vizepi、MiheevN、LCRW、VarvatosVex、herocrab、artkez、Erdroy、brbrmensch、thallard

合并的 PR：62

* 添加了 **Nintendo Switch 支持**（即将向注册开发者提供）
* 在 Game Cooker 中添加了对独立数据和代码输出目录的支持
* 在 C++ 脚本中添加了对数组的初始化列表支持
* 添加了 **车辆支持**
* 当尝试从过大的文件数据加载字节时添加错误日志
* 添加了 MeshBase 作为 Mesh 和 SkinnedMesh 的共享实现部分
* 添加了从蒙皮模型生成碰撞数据的支持
* 在刚体下使用三角形网格碰撞体时添加警告
* 添加了在烘焙碰撞数据时遮罩材质槽的支持
* 在脚本 API 中添加了对 `string` 常量作为默认值的支持
* 添加了 **本地化** 支持和工具
* 添加了导入 `.po` 文件的支持
* 添加了 `LocalizedStringTable` 资源类型
* 添加了 `LocalizationSettings`
* 添加了 `LocalizedString`
* 向 C++ API 添加了 `CultureInfo`
* 添加了 `GOLDEN_RATIO` 常量
* 添加了 **新编辑器图标**
* 添加了从 base64 编码字符串反序列化字节数组
* 添加了使用 `AssetsImportingManager` 将资源导入自定义格式的支持
* 在游戏代码中添加了自定义资源类型工厂的支持
* 在脚本 API 中添加了在 Dictionary 内部嵌套 Array 的支持
* 在脚本 API 绑定代码中添加了对使用泛型类值类型的支持
* 添加了导入带有字符串本地化的 `.po` 文件
* 在 C#/C++ 代码中搜索本地化字符串
* 在场景和预制体中搜索本地化字符串
* 添加了将本地化导出为 `.pot` 文件以进行翻译
* 添加了带有选择器的 `CultureInfo` 编辑器
* 为 Json 资源编辑器窗口添加了撤销支持
* 添加了字符串本地化 API
* 为 UI 添加了 LocalizedString 支持
* 向编辑器工具添加了 `CreateSearchPopup`
* 向 `TextRender` 添加了 `LocalizedString` 以支持本地化文本
* 添加了使用动画图与不同蒙皮模型的动画模型的支持
* 向动画模型添加了 `FindClosestNode`
* 添加了 `Keyboard.IsAnyKeyDown`
* 为 GPU 资源添加了缺失的名称
* 添加了 **低级网络**
* 为点/聚光灯渲染添加了深度测试，以优化远距离灯光渲染
* 添加了 `HasTypedUAVLoad` 到 GPULimits（用于 GPU 光照贴图支持烘焙检测）
* 为 D3D12 添加了 DRED 和可选的基于 GPU 的验证
* 为 D3D12 验证添加了 UAV 槽位维度缓存
* 在 D3D12 上渲染散景景深（修复后工作正常）
* 在集合中添加了背景颜色（嵌套集合的可读性更好）
* 在集合编辑器中添加了对 `NotNullItems` 选项的支持
* 在脚本 API 事件中添加了对使用指针的支持
* 在脚本 API 事件中添加了对使用引用的支持
* 向可视化脚本添加了 **延迟节点**
* 在编辑器预览中添加了时间线位置与动画播放的同步
* 在编辑器失焦时向编辑器休眠阶段添加了性能分析器睡眠事件
* 为 C++ 添加了 `Nameof` 辅助工具
* 在首次添加新设置资源后自动设置游戏设置
* 添加了带有纹理组配置的 **流式传输设置**
* 添加了在烘焙游戏时剥离纹理质量的支持（按平台）
* 添加了基于可见性的 **动态纹理流式传输**
* 在材质中添加了对使用每纹理组采样器的支持
* 向 json 值添加了 `GetStringAnsiView`
* 添加了在内容窗口中打开所有选定资源的选项
* 添加了在原生 json 读取器中加载 C# 字典的支持
* 为编辑器对话框中的类型分组添加了 `Category` 特性
* 在编辑器的图形质量窗口中添加了纹理组预览
* 添加了在编辑器中编辑纹理组的支持（无需重新导入）
* 在着色器中添加了对 **可编程采样器** 的支持
* 向动画模型添加了 `AnimationUpdated` 事件
* 向动画模型添加了 `SetCurrentPose`
* 为模块化角色向动画模型添加了 `SetMasterPoseModel`
* 为脚本重命名添加了数字检查
* 添加了 **Tracy 性能分析器** 支持
* 向引擎 API 添加了 `SceneTicking`
* 添加了 `-debugwait` 命令行参数，以便在引擎启动时等待 C# 调试器附加
* 为凸网格添加了物理查询
* 添加了编辑器选项以自定义新编辑器窗口的默认位置
* 为 C# 方法调用添加了自动性能分析事件
* 为可视化脚本方法调用添加了自动性能分析事件
* 基于选定项目的工具提示为 ComboBox 添加了自动工具提示
* 添加了在游戏视口中隐藏 GUI 的选项
* 向 SceneAnimationPlayer 添加了 `MapTrack`
* 向 ProbesRenderer 添加了 GPU 性能分析事件
* 向 `Actor` 添加了辅助 `FindScript<T>` 和 `FindActor<T>`
* 为不使用撤销的属性添加了 `NoUndo` 特性
* 添加了在游戏视口中显示调试绘制形状的选项
* 通过上下文菜单添加了从 Visject 框复制/粘贴值的选项
* 为构建工具包含缓存添加了使用库样式包含的支持
* 为 UI 添加了 **9-slicing 支持**，用于绘制精灵和纹理
* 在脚本中跳过了为私有字段生成结构字段访问器
* 通过画笔在 UI 中绘制纹理和精灵时，添加了使用点采样的选项
* 为可视化脚本添加了 `Int16` 和 `Uint16` 脚本/序列化支持
* 在顺序包构建之间保留了包输出目录
* 为编辑器添加了 `-play` 命令行参数以运行游戏（可选场景 ID 参数值以播放特定关卡）
* 为引擎部署添加了对自定义包输出目录的支持
* 为引擎部署添加了对自定义构建配置的支持
* 在 Github Actions 上添加了 **每日主分支构建** 作为持续部署
* 为场景树项目添加了更多缩进，使其更易于阅读
* 从自定义资源项目向关卡/预制体视口添加了自定义拖放支持
* 在播放时聚焦游戏窗口
* 添加了 **作业系统**
* 在编辑器的图形质量窗口中添加了植被密度缩放选项
* 在脚本中添加了对 `Function<>` 作为方法参数的支持
* 为 Margin 属性添加了顺序
* 为导航系统的场景数据添加了 `SceneNavigation`
* 添加了 **任务图**
* 添加了使用秒作为时间线/媒体持续时间的选项
* 添加了 `Engine::UpdateGraph` 用于异步引擎/游戏更新
* 通过任务图添加了异步动画更新
* 通过任务图添加了异步粒子更新
* 在 `Dictionary` 中添加了对分配器类型的支持
* 在不活动时在帧之间添加了空闲休眠以节省 CPU 周期
* 添加了记录 Windows 构建号
* 添加了 `RingBuffer` 模板
* 在 `Dictionary` 中添加了对分配器类型的支持
* 在 *Flax.Build* 性能分析工具中，添加了 `Stopwatch` 代替 `DateTime.Now` 以提高精度
* 向视点添加了键盘快捷键（数字键盘）
* 添加了 `PLATFORM_THREADS_LIMIT` 以限制最大并发数
* 添加了 MemoryCompare、MemoryClear 和 MemoryCopy，用于在 C# 中直接内存访问
* 在编辑器性能分析器中忽略来自性能分析工具的内存分配
* 在构建工具清理命令中添加了中间数据文件夹删除
* 添加了 `Actor.RotateAround`
* 在为 C# 生成脚本绑定胶水代码时添加了自动命名空间 using 收集
* 为具有指定限制的浮点值输入字段添加了自动滑块速度
* 添加了 Alpha Panel 控件
* 为 Vulkan 添加了对间接绘制的支持
* 为 Vulkan 添加了 GPU 粒子支持
* 在 Vulkan 上添加了对渲染到 3D 纹理的支持
* 在 Vulkan 上添加了对几何着色器的支持
* 在视口中生成 Actor 时添加了增量 Actor 命名
* 为 PS4 添加了网络实现
* 为材质常量绑定代码添加了安全检查，以防止无效内存访问
* 添加了 `GPUContextVulkan::CopySubresource`
* 为性能跟踪添加了更多性能分析事件
* 添加了 `Content.Stats` 用于资源统计信息（取代 `Content.AssetCount`）
* 添加了 `Streaming.Stats` 用于资源流式传输统计信息
* 为方向光阴影添加了视图捕捉模式以提高稳定性
* 添加了预加载初始编辑器场景以提高启动性能
* 向 Button 添加了对自定义背景画笔的支持
* 添加了 `DebugDraw::DrawTube`
* 添加了 `Rectangle.Distance`
* 添加了预览单个网格 UV 的选项
* 向动画详细信息添加了内存使用信息
* 为批处理调试形状绘制添加了更多辅助方法
* 添加了 **物理碰撞体调试视图** 模式，以显示物理世界形状（实体）
* 向动画模型添加了 `UpdateSpeed` 以支持播放速度缩放
* 为动画预览播放/暂停和速度更改添加了小部件
* 添加了在运行时为单个动画播放创建虚拟动画图的支持
* 添加了地形物理碰撞调试绘制支持
* 添加了模拟光照以改善调试绘制实体形状渲染的可读性
* 向动画窗口添加了带有蒙皮模型的预览面板
* 添加了对后缓冲区 **渲染百分比缩放与升采样** 的支持
* 为通过自定义 Actor 列表渲染的模型添加了剔除和渲染遮罩检查
* 当目标缺失时，在构建工具中添加警告日志
* 添加了带返回值的 `JsonSerializer.ParseID`
* 添加了由 `GamePlugins` 捆绑自定义资源的支持
* 添加了 `CustomUpscale` 后期处理效果，以用自定义脚本替换升采样器
* 向 C# 添加了 Vector3 的 `ClosestPointPointLine`
* 向 FlaxDocs 添加了更多文档
* 添加了对 Rider 2021 的支持，使用最新检测到的 Rider 版本
* 添加了使用最新检测到的 Rider 版本
* 添加了在使用变换 Gizmo 移动对象时，当鼠标在使用期间移出视口时仍可移动的支持
* 添加了 `BoundingBox.MakeScaled`
* 在编辑器预览中，为动画模型骨骼绘制添加了使用即时 DebugDraw
* 在 C# 脚本 API 中，为默认值特性添加了 `Vector2/3/4` 和 `Quaternion` 常量的支持
* 添加了在编辑器预览中显示动画模型边界的选项
* 添加了在编辑器预览中显示模型边界的选项
* 添加了 AssetPreview 在需要时使用调试绘制的支持
* 添加了在编辑植被实例时聚焦其边界的支持
* 添加了在绘制模型顶点时聚焦模型的支持
* 在哈希映射和字典中添加了对对象引用包装器使用的支持
* 改进了 `Int2/3/4` 的序列化
* 添加了 `Vector2/3/4` <=> `Int2/3/4` 的构造函数
* 为 `Int3/Int4` 添加了运算符
* 为 `Int2/3/4` 添加了常用函数
* 添加了一些启动画面引用
* 为插件图标添加了可选位置，位于插件项目根目录内
* 添加了 `MeshDataCache` 工具，用于异步网格数据缓存
* 在编辑器窗口中为动画时间线添加了播放/暂停/停止按钮
* 为时间线导航栏按钮添加了色调
* 为带有局部骨骼节点姿态的动画图连接添加了骨骼图标
* 添加了对使用结构作为可视化脚本和动画图参数的支持
* 在动画图中添加了对对象和枚举属性的支持
* 添加了通过定义按平台禁用 Vulkan 计时器查询的支持
* 在编辑器预览中添加了预览模型网格法线和切线的选项
* 向刚体添加了 `AddForceAtPosition`
* 向 Visject 新参数类型选择器列表添加了完整文档工具提示
* 在使用组面板的值编辑器上添加了对 `VisibleIf` 的支持
* 添加了使用 `AssetsImportingManager` 将资源导入自定义格式的支持
* 在可视化脚本中添加了强类型 *This* 节点输出
* 在编辑器 UI 中添加了超平滑的四元数编辑
* 在 EnumComboBox 中添加了对 `int64` 值的支持
* 在 CustomEditor 中添加了对自定义值传播的支持
* 向 UI 控件添加了 `LocalLocation`
* 添加了在主线程上从模型资源烘焙碰撞数据的支持，并修复了网格在流式传输期间的问题
* 在动画图中添加了对 Is Null、Is Valid、As、Is 和 Type Reference 节点的支持
* 添加了 `C++ Json Asset` 和 `C++ Function Library` 模板
* 改进了对控件锚点编辑的支持，具有边界保留和枢轴设置（Shift 和 Control 修饰键）
* 当打开场景动画窗口时添加了加载信息文本
* 在编辑器插件中添加了重用 `UIControlControlEditor` 和 `AnchorPresetsEditorPopup` 的支持
* 在 C# 绑定代码中添加了来自另一个模块的字段命名空间的支持
* 在绑定中为使用的命名空间添加了排序，并包含嵌套类型检查
* 在头文件中添加了缺失的包含
* 添加了相对于枢轴点的 UI 控件位置编辑
* 向导航网格属性添加了 `DefaultQueryExtent`
* 在编辑器中添加了删除精灵的选项
* 为更好的可读性，添加了时间线轨道预览值的自动调整大小
* 在动画图中添加了对 Pack/Unpack Structure 节点和 Enum 常量的支持
* 添加了对 Json 序列化的 C# 类型的 OnSerializing、OnSerialized、OnDeserializing 和 OnDeserialized 回调的支持
* 将默认的 AllocatePages/FreePages 实现从 Unix 移至 PlatformBase
* 将 JsonConverters 移至单独的文件
* 重新实现了 **具有更少重影和更好质量的 TAA**
* 改进了 Rider 安装的检测
* 为 Mesh 实现了 DownloadDataCPU 并添加了结果条目计数
* 改进了动画预览的可用性以用于调试
* 改进了脚本类型的工具提示生成
* 改进了时间线帧标签的显示
* 增加了滚动条尺寸以便于使用
* 在滚动条使用之前，增加了 Visject 辅助上下文菜单的项目限制
* 优化了仅 C# 项目引用的构建
* 优化了具有许多轨道的复杂时间线的 UI
* 当不使用颜色混合时，优化了 Vulkan Framebuffer
* 优化了性能分析器窗口的更新，仅刷新可见选项卡的 UI
* 优化了性能分析器窗口的 UI
* 优化了调试日志窗口新条目的添加（手动放置而不是完全布局）
* 优化了 `BoundingSphere::FromBox`
* 优化了用于大量单帧调试形状和线条的调试绘制
* 优化了调试绘制线框球体以使用自动 LOD
* 优化了流式传输服务，使用任务图在作业系统上异步更新
* 优化了 Vulkan 着色器编译器的内存分配
* 优化了 ContentStorageManager，使用任务图在作业系统上异步更新
* 优化了 Vulkan 后端的内存分配
* 优化了物理模拟 Actor 活动变换的刷新
* 优化了物理模拟碰撞对缓存
* 通过使用原始 json 值写入，优化了植被实例序列化
* 优化了各种引擎代码
* 优化了 `Foliage::OnFoliageTypeModelLoaded` 仅重建单个植被类型集群
* 使用 StringView 优化了基本文件路径操作的 String 使用
* 通过在 Windows 上使用 Roslyn C# 编译器服务器，优化了 C# 脚本编译时间
* 通过直接的 Mono 调用，优化了绑定中的 C# 方法调用
* 优化了托管内存分配
* 优化了 `ThreadLocal.h` 中的包含
* **优化了场景渲染**（数据缓存等）
* 使用基数排序优化了 CPU 粒子排序
* 优化了 C# `Actor.GetChildren<T>()` 和 `Actor.GetScripts<T>()`
* 优化了动画模型骨骼矩阵缓冲区更新
* 优化了脚本对象数据反序列化
* 使用按植被类型的四叉树集群优化了植被
* 使用手动实例化绘制调用批处理优化了植被渲染
* 引擎各部分的优化
* 使用 `RenderListAllocation`（内存池）优化了渲染器内存分配
* 移除了未使用的 `WindowsManager::CalcCenteredWinPos`
* 从 Json Asset 选择器对话框中移除了 UI 控件
* 从 `MAssemblyOptions` 中移除了未使用的 `DictionaryInitialSize`
* 重构了编辑器中选择聚焦的逻辑
* 将 `FontReference` 重构为引用类型（类）
* 重构了基础类型初始化，在 API 处理期间完成
* 重构了 AnimGraph 以支持异步执行
* 重构了 CPU 粒子以支持异步执行
* 重构了 FileSystemWatcher 接口以使其更简单
* 重构了 AnimGraph 调试流以使用脚本 API 事件（更快）
* 重构了 Variant 类型 ManagedObject 序列化，使其可用于可视化脚本
* 将时间设置中的默认更新 FPS 更改为 60
* 将性能分析器样本长度更改为 60hz 下的 10s
* 将所有 Flax 示例更改为 60 FPS
* 更改为不在日志文件中记录计算机/用户名
* 将 `CalculateTangents` 默认值更改为 false，并在缺失时默认计算法线/切线
* 更改了 `WindowBase` 以允许为没有高级窗口功能的平台提供更好的代码共享
* 将 `fmt` 更新到版本 6.2.1（2020年5月9日）
* 修复了 Linux 上的虚拟语言区域
* 修复了带有子控件的 TextBoxBase 的使用
* 修复了格式字符串错误断言为软检查
* 修复了字符串视图的 natvis 显示
* 修复了集合编辑器中的间距处理，以正确对齐标签与边距
* 修复了 `JsonAssetProxy.IsProxyFor`
* 修复了使用嵌套场景动画递归时的堆栈溢出
* 修复了生成的工具提示描述中反斜杠的转义
* 修复了构建设置限制
* 修复了在编辑器中重新设置 Actor 父级以保留世界位置
* 修复了动画模型的包围盒计算
* 修复了工具提示受显示器边界约束，而不是虚拟桌面或父窗口边界
* 修复了可视化脚本中的 Is Null 和 Is Valid 节点以处理托管实例比较
* 修复了卸载已卸载场景时的崩溃
* 修复了使用自动大小时的 Label 文本定位
* 修复了在调试期间在 Visject 中创建绕行节点
* 修复了在烘焙开始时使用缓冲区清除光照贴图纹理时缺失的 `mipIndex`
* 修复了选择来自已卸载场景的 Actor 时的错误
* 修复了在编辑器插件窗口中显示游戏插件
* 修复了为 3D UI 渲染禁用顶点捕捉
* 修复了编辑器 UI 树控件单击以先处理选择更新
* 修复了更改编辑器视图方向时的摄像机位置
* 修复了在 Z 轴移动的正交视图中的 Gizmo（以及其他垂直视图组合）
* 修复了在场景树窗口中显示上下文菜单时使用滚动的问题
* 修复了内容数据库中插件项目根节点中的路径
* 修复了导入具有多个材质但几何体上没有映射的模型网格时的崩溃
* 修复了 CharacterController 在运行时被禁用后仍然碰撞的问题
* 修复了如果模型分配了无效材质槽索引时编辑器 UI 中的异常
* 修复了使用单轴时 Gizmo 的稳定性
* 修复了如果布局边界宽度等于行宽，启用换行的文本处理
* 修复了重叠控件的 `Control.GetChildAtRecursive`
* 修复了从流中读取空字符串时的崩溃
* 修复了将粒子发射器添加到系统时间线以具有完整持续时间
* 修复了 Linux 和 Android 上原生线程的命名
* 修复了重载虚拟资源时的崩溃
* 修复了表面节点值修改的误报
* 修复了 AssetPicker 中处理无效资源而不是抛出异常
* 修复了 `ForwardShadingFeature` 中的缓冲区溢出崩溃
* 修复了为使用深度淡出节点的粒子绘制阴影
* 修复了使用缩放的局部骨骼 TransformBuffer 导入模型动画曲线
* 修复了 `Pair` 中缺失的运算符和构造函数
* 修复了资源路径来自项目而不是包
* 修复了从引擎二进制文件或项目根目录以外的文件夹加载二进制模块的原生依赖项
* 修复了右键单击时停靠窗口的自动聚焦，以防止在上下文菜单中使用时游戏窗口鼠标被窃取
* 修复了 `String::Resize` 以支持缓冲区增长
* 修复了在编辑器运行模式中游戏暂停时捕获鼠标焦点
* 修复了对 `RichTextBoxBase` 内部缓冲区的越界访问
* 修复了如果包含数组字段，脚本 API 中的 POD 结构检查
* 修复了一些性能分析事件名称
* 修复了字体失效时的崩溃
* 修复了来自其他线程的 C# 性能分析事件
* 修复了创建后并在场景树中使用编辑器快捷键重命名选中的 Actor
* 修复了着色器中的次要警告
* 修复了 Linux 上的语言区域
* 修复了文件对话框更改工作目录的问题（在 Windows 上）
* 修复了在特定系统语言区域配置上的编译问题（构建工具问题）
* 修复了 Windows 上调试符号的同步
* 修复了在脚本 API 字段中使用固定数组初始化器
* 修复了失败时上下文菜单搜索中的错误
* 修复了 `ThreadLocal` 使用原子操作并防止罕见的竞态条件
* 修复了在 Windows 上使用 STD 控制台输出运行 Flax
* 修复了 Margin 属性序列化时编辑器中的撤销错误
* 修复了打开 Visual Studio 项目项目时检查路径以防止创建虚拟文件
* 修复了脚本虚表覆盖中基类方法调用的处理
* 修复了编辑器运行模式中游戏插件的初始化顺序，以模拟独立游戏
* 修复了也检查引用项目的最低引擎版本
* 如果编辑器中的游戏二进制文件仍然缺失，则重新编译
* 修复了将包含结构内部对象引用的数组传递给托管世界时的罕见崩溃
* 修复了通过虚表条目调用具有多个参数的方法的覆盖脚本对象的基类
* 修复了动画模型边界计算以使用骨骼位置
* 修复了树中跳过不可见节点的范围选择
* 修复了预制体的 C# 字典反序列化
* 如果流中未指定 `ParentID`，修复了脚本反序列化
* 修复了更改预制体根
* 修复了自定义序列化对象（例如 LocalizedString）的撤销
* 修复了新 json 资源缺失的资源注册
* 如果基类型不可序列化，修复了 `API_AUTO_SERIALZIATION` 的使用
* 修复了 `StringView` 中 `StartsWith` 和 `EndsWith` 的大小写敏感性检查
* 修复了在 API 事件中使用 `Delegate<>`
* 修复了 `TypeSearchPopup` 显示基类型被隐藏的类型
* 修复了脚本模板不抱怨文档警告
* 修复了多行 xml 文档注释的工具提示特性生成
* 修复了变换节点/骨骼在替换模式下变换为单位矩阵时的问题
* 修复了 `protected internal` 访问修饰符的使用
* 修复了文本框错误
* 修复了在预制体中使用 ScreenSpace 画布时编辑器视口输入
* 修复了 `ScreenSpace` UICanvas 预制体预览的激活/停用
* 修复了粘贴 UI 控件时的顺序
* 修复了预制体预览中 ScreenSpace UICanvas 的顺序（移回以防止编辑器小部件遮挡）
* 修复了可视化脚本方法覆盖节点的自动大小
* 修复了 D3D12 上着色器的描述符绑定
* 修复了 D3D12 上缺失的 UAV 屏障
* 修复了 D3D12 上的 GPU 同步
* 修复了 D3D12 资源状态转换屏障
* 修复了 D3D12 上的光照贴图烘焙
* 修复了在脚本类中的数组字段上使用自定义分配器
* 修复了编辑字典键值后的 UI
* 修复了各种编辑器 UI 图标问题，以提高可靠性和设计
* 修复了 `Asset::ToString()` 不依赖于引用计数并看起来更简单
* 修复了为脚本生成 C++ 胶水代码中的包含列表（非 POD 和变体包装器可能产生额外的包含）
* 修复了 Blend Space 2D 使用修正的叠加混合
* 修复了块压缩纹理的纹理流式传输最小 Mip 加载
* 修复了如果在启动时禁用，启用后的样条线模型可见性
* 修复了脚本 API 中数字和枚举的默认值生成
* 修复了脚本 API 中带有内联 summary 标签的文档注释解析
* 修复了使用编辑器菜单选项时支持异步打开的脚本项目打开
* 修复了在 C# 脚本中使用 Keyboard 和 Mouse 对象
* 修复了缓存字体使用以使用正确的非托管对象验证检查
* 修复了在编辑器中选择多个不同 UI 控件
* 修复了由于 `SamplesBuffer::Add` 错误导致的崩溃
* 修复了在预制体中更改 UIControl 类型时的错误
* 修复了 Render2D 中模糊强度过大导致的崩溃
* 修复了 `WeakAssetReference` 处理资源卸载
* 修复了 `Asset::WaitForLoaded` 中非常罕见的死锁情况
* 修复了地形法线映射问题
* 修复了在可视化脚本中使用嵌套类型
* 修复了重新导入精灵图集时的崩溃
* 修复了编辑后的精灵图集序列化
* 修复了在编辑器中命名新精灵
* 修复了模型 UV 预览绘制
* 修复了 Actor 复制后 ActorTreeNode 顺序的同步
* 修复了胶囊物理查询中缺失的旋转参数
* 修复了由于结构填充，Vulkan 上 BitonicSort 常量缓冲区大小错误
* 修复了 Vulkan 上结构化 UAV/SRV 缓冲区的使用
* 修复了无效数据反序列化期间的崩溃
* 修复了仅获取属性在编辑器中不显示的问题

# 植被编辑

![植被编辑](/manual/media/foliage-painting.gif)

要在你的关卡中绘制植被，你需要选择 **绘制** 选项卡，并使用编辑器视口用笔刷绘制植被。你可以在属性面板中调整 **笔刷大小**，以及选中的植被类型放置和绘制属性。

使用 *鼠标左键* 在对象表面上绘制，或按住 *Control* 键移除实例。

要使用给定的植被类型子集进行绘制，请使用植被类型列表中的复选框来选择在绘制期间要使用的类型。
